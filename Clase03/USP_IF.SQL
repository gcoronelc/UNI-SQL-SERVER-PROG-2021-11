
CREATE PROCEDURE USP_IF(@NOTA INT)
AS
BEGIN
	-- VARIABLES
	DECLARE @CONDICION VARCHAR(20);
	-- PROCESO
	IF(@NOTA>=12)
		SET @CONDICION = 'APROBADO';
	ELSE
		SET @CONDICION = 'DESAPROBADO';
	-- REPORTE
	PRINT CONCAT('CONDICION: ', @CONDICION);
END;
GO

EXEC USP_IF 16 ;
GO